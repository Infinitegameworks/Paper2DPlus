[FilterPlugin]
; This section lists additional files which will be packaged along with your plugin. Paths should be listed relative to the root plugin directory, and
; may include "...", "*", and "?" wildcards to match directories, files, and individual characters respectively.
;
; Examples:
;    /README.txt
;    /Extras/...
;    /Binaries/ThirdParty/*.dll

; DefaultPaper2DPlus.ini carries redirects and plugin defaults required by installed projects.
/Config/...

; Shaders/Private/*.usf must be staged so the three global shaders (Paper2DPlusAppearanceComposite)
; compile in installed/Fab builds â€” AddShaderSourceDirectoryMapping resolves this directory at runtime.
/Shaders/...
